{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Kosten</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Bedrag</th>
                <td>{{ entity.bedrag }}</td>
            </tr>
            <tr>
                <th>Gratistarief</th>
                <td>{{ entity.gratistarief }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('kosten') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('kosten_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
